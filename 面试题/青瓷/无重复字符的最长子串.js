/**
 * @param {string} s
 * @return {number}
 */
var lengthOfLongestSubstring = function (s) {
    const map = {};
    let left = 0;
    while(left < s.length) {
        for(let i = left; i < s.length; i++) {
            if(s[i] != s[i+1]) {
                
            }
        }
    }
};